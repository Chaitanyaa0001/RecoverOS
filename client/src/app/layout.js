import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "RecoverOS",
  description: "RecoverOS is a revenue recovery platform that helps businesses identify and recover lost revenue opportunities. It provides insights, analytics, and tools to optimize revenue streams and improve financial performance.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
   
        {children}
      </body>
    </html>
  );
}
